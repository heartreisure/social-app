NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_DEMO_USER_ID=
# Optional: if your auth flow stores a custom access token key,
# mirror it into localStorage["sb-access-token"] for profile lookup.
